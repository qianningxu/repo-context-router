interface:
  display_name: "Repo Doc Router"
  short_description: "Install Codex doc routing files in a repo."
  default_prompt: "Use $repo-doc-router to initialize repo doc routing in this repository."
